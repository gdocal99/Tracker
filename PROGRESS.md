# BLOCK 3 — журнал обновлений

Ведётся построчно. Незакрытые пункты переносятся в следующий заход.

## Философия (закреплено)
- Один файл `index.html` (~140KB), чистый JS/CSS/HTML. Никаких сборщиков/фреймворков.
- Код функций выдаётся целиком, без заглушек `// ...`.
- Синхронизация: только LWW по `lastUpdated`. Никакого построчного слияния.
- СДВГ-минимализм: тихая гавань, без sticky, без визуального шума, самооценка — внизу.
- Ретро-ввод (`badLogging`): не писать фейковые времена, в отчёте вырезать часы/минуты.

---

## Итерация 2 — 2026-07-15

### Сделано
- [x] 1. БАД-сплит: `supsLoggingHTML(day)` (главный экран, Здоровье) + `supsConfigHTML()` (настройки, Таб 2).
- [x] 2. Удалена `viewToggleHTML()` и её вызовы. Переключение вида — только в настройках (Таб 1).
- [x] 3. Ритуалы (regime) разделены на «☀️ Утренние» (bed, warmwater, teeth) и «🌙 Вечерние» (phone). Без скрытия по времени.
- [x] 4. `markHTML(day)` перенесён вниз `col-main`, под все категории.
- [x] 5. `badLogging`: поле в `blankDay`; кнопка-тумблер «Наспех» в `markHTML` (рамка #CBA45C); `toggleItem`/`togglePrayer` не пишут время при активном флаге; `buildExport` — плашки «ПРОПУЩЕН»/«НАСПЕХ» и вырезание таймстампов задач/намазов.
- [x] Вода: динамическая надбавка +500 мл, если средняя дневная температура (из фоновой погоды) > +25°C.
- [x] Ревизия C1: пробелы в HUD-строке статистики (было «0намаз», «45без срывов», «50.4%жизни»).
- [x] Ревизия C3: логика телефона упрощена — баллы начисляются в свой день, убрана завязка на `days[k+1]` и штрафы задним числом.

### Последствия / на заметку
- Упрощение телефона пересчитывает исторический баланс: раньше баллы за телефон уходили на предыдущий день, теперь — на день галочки. Разовый сдвиг в цифрах ожидаем.

### Ожидают вводных
- [ ] Ревизия, детали №2 и №4 — в присланном тексте отсутствовали. Нужны формулировки.

---

## Итерация 3 — 2026-07-16

### Сделано
- [x] Ползунки 1–5 (sliderHTML): шкала min=1/max=5, подсказки-ориентиры под каждым (energy/focus/spirit/drift/control). Обратная совместимость: старые значения >5 делятся на 2 (norm5) при отображении и в отчёте.
- [x] Детализация срывов: поля slipType ("fantasy"/"porn"/"both"/"planned") и slipFirst ("porn_first"/"fantasy_first") в blankDay; кнопочный опросник slipDetailHTML вместо textarea; подвыбор «что первое» только для both; интеграция в buildExport («Порно + Фантазии, первым возникло: порно», «плановый вечерний срыв»).
- [x] % прожитой жизни округлён до 1 знака (HUD lifePct + life-card) вместо 3.
- [x] Ритуалы: заголовки переименованы в «☀️ Утренний блок» / «🌙 Вечерний блок».
- [x] Отчёт: ползунки выводятся в шкале /5 (norm5) вместо /10.

### Перепроверено (уже было в итерациях 1–2, подтверждено тестами)
- Предохранитель штрафов: пустой день и день с <3 разделов не уходят в минус (loggedSections>=3 → closed).
- Телефон начисляется в свой день, без завязки на days[k+1].
- LWW: setDay ставит lastUpdated=Date.now(); mergeDayDeep заменяет день целиком по большему таймстампу.
- viewToggleHTML удалена; БАД-сплит; markHTML внизу; badLogging.

### Доставка
- Пользователь деплоит через Netlify Drop (ручной drag&drop файла) — репозиторий с сайтом НЕ связан. Готовый index.html отправляется в чат для ручной загрузки в Netlify.

---

## Итерация 4 — 2026-07-16 (пакет из аудита: B1 + B3 + B5 + UI + чистка)

### Сделано
- [x] B1: buildExport подтягивает старый текстовый триггер срыва — `[Старая запись: …]` рядом с новой кнопочной сводкой.
- [x] B3: ползунки — введён `sliderSet()` (задано = число > 0); пустое/null/старый 0 → `.unset` «не отмечено», не 1. В отчёте 0 → «—». `norm5` конвертирует только заданные значения.
- [x] B5: applyImport пропускает импортируемые дни через `repairDay()` (и при add, и при merge) — восстановление массивов, защита от неполной структуры бэкапа.
- [x] Чистка: удалены мёртвые `PHONE_PTS`, `setView()`, `emptyScalar()`, `TEXT_FIELDS` (0 остаточных ссылок).
- [x] UI: три кнопки самооценки (Идеальный/Провал/Наспех) в один `mark-row`; «Плановый» срыв — золотой (#CBA45C) вместо зелёного; из подвала убран текст «Авто-скачивание отключено. Сделай копию.».

---

## Итерация 5 — 2026-07-17 (голосовой запрос: сохранность, работа, БАД-курсы, утро/вечер)

### Аудит сохранности (без изменений логики, разъяснено пользователю)
- Разные дни между устройствами никогда не конфликтуют; LWW заменяет день целиком по большему `lastUpdated`. Риск только при правке ОДНОГО дня на обоих устройствах офлайн (побеждает сохранённый позже). Зависит от часов устройств. Google Drive без атомарной блокировки — самочинится на следующей синхронизации.

### Сделано
- [x] Защита паха: ежедневный вечерний пункт `groin` в regime (вечерний блок), pts 1, pen 1.
- [x] Блок «Работа»: `workLog:[{from,to}]`, несколько смен за день, сумма часов (с учётом перехода через полночь), в отчёте «Работа: 08:20–13:00, 16:00–21:00 (всего 9.7 ч)». Только запись — на баллы/серии НЕ влияет. Размещён в деталях дня.
- [x] БАДы: блок сворачивается (`supsOpen`); курсы «пью / на паузе» с датами (`meta.supPeriods`), пауза уводит БАД в свёрнутый архив «💤 На паузе», кнопка «▶ Возобновить»; показ длительности курса («идёт N дн», «курс N дн · дата–дата»). KPI здоровья считает только активные БАДы (`activeSups`). Дозы истории — по дням как раньше.
- [x] Оформление утро/вечер: карточки-подблоки `.daypart` (☀️ Утро — золотой акцент, 🌙 Вечер — синий) вместо простых подписей.
- [x] Синхронизация усилена: детерминированный тай-брейк в `mergeDayDeep` при равных метках (сходимость обоих устройств); досинхронизация при сворачивании вкладки (`visibilitychange` → hidden).
- [x] Версия 0.45.0 / билд 17 июля 2026 (визуальный маркер новой сборки).

### Ожидают вводных
- [ ] БАД-курсы и «Работа» реализованы с дефолтами (пауза+даты; работа без баллов) — интерактивный вопрос не прошёл, ждём подтверждения/корректировки.
- [ ] Оформление утро/вечер — сделан первый вариант карточками; готов переделать под другую идею.
- [ ] Ревизия, детали №2 и №4 (из старого запроса) — так и не присланы.

---

## Итерация 6 — 2026-07-17 (дашборд, зубы/массаж, чекины)

### Сделано
- [x] Зубы и массаж разделены: `teeth` → «🌙 Зубы (перед сном)» (вечер); новый утренний `facemassage` «☀️ Массаж лица (лимфодренаж)». Дубль «Зубы + массаж» убран из «Тело».
- [x] Новый `massage_pro` «Массаж (сеанс у специалиста)» в «Тело» с выбором зоны (лицо/спина/шея/общий/ноги).
- [x] `expander` перенесён из «Тело» в вечерний ритуал. Вечерний блок: телефон · защита паха · зубы · экспандер. Утро: кровать · тёплая вода · массаж лица.
- [x] Два чекина в «Самоконтроль»: `⏰ Дневной чекин (12:00)` и `⏰ Вечерний чекин (22:30)` — обычные галочки, +1 балл, без штрафа.
- [x] Единый Дашборд (`dashHTML`): кольцо, дата с навигацией, баланс, спарклайн 7 дней, тонкая линия жизни (6px), серии — одной карточкой (#12180F, radius 20px). Не липкий. Старые topbar/hud/life-card и 14-дневная полоса дат убраны (навигация — стрелки + нижняя история).
- [x] Спарклайн (`sparkHTML`): текстовые блоки ▁–█ по obPct за 7 дней, пустые дни — ░, цвет по уровню.
- [x] Версия 0.46.0.

### Интерпретации (поправь, если не так)
- «Массаж лица» и «лимфодренаж утром» объединены в один утренний пункт.
- Экспандер и зубы перенесены из «Тело» в ритуалы (убраны дубли), чтобы не двоились.
- 14-дневная кликабельная полоса дат заменена спарклайном (переход по дням — стрелками ‹ › и нижней историей).

---

## Итерация 7 — 2026-07-17 (лимфодренаж/массаж раздельно, зарядка, чекины, пропущенные дни, 3 карточки, намазы в шапку)

### Сделано
- [x] Ритуалы переразбиты: утро — Зубы (утро), Тёплая вода, Лимфодренаж (отдельно), Зарядка; вечер — Телефон, Защита паха, Зубы (вечер), Массаж шеи/лица, Экспандер. Массаж лица и лимфодренаж теперь РАЗДЕЛЬНЫ.
- [x] Зарядка (`charge`): пресс/отжимания/приседания с вводом повторений; засчитывается, если хотя бы одно ≥ 20 (авто-ставит `items.charge`). Функции chargeDone/setCharge/chargeHTML.
- [x] Пропущенные дни (Задача 1): `isSkippedDay(dk)` — только ПРОШЛЫЙ день (dk<today) в диапазоне с TRACK_START=25 июня и loggedSections<5; в календарной сетке серый пунктир + ✕. Текущий/будущие НИКОГДА не помечаются.
- [x] 3 карточки в правой колонке (Задача 4): «📊 Логистика дня» (хронология, смены, еда, доход, сон, стул), «🧠 Ментальное состояние» (срыв + 5 ползунков + urge), «✍️ Рефлексия» (5 текстовых полей).
- [x] Намазы в шапку: 5 кликабельных пилюль (Ф З А М И) + счётчик N/5 в дашборде — шапка больше не пустая.
- [x] Перепроверены (задачи 2/3/6): LWW + repairDay при импорте; дашборд/спарклайн/toFixed(1); мёртвый код (PHONE_PTS/setView/emptyScalar/TEXT_FIELDS) отсутствует; кнопки срыва + Плановый золотой + слияние slipTrigger; markHTML 3 в ряд; текст «Авто-скачивание» удалён.
- [x] Версия 0.47.0.

### Интерпретации (поправь)
- teeth разделён на `teeth_am` (утро) и `teeth` (вечер) — чистка зубов дважды.
- Массаж лица/шеи → вечер (по большому ТЗ), лимфодренаж → утро (по голосовому). Если массаж лица нужен утром отдельно — скажи.
- Зарядка = один пункт с тремя упражнениями; засчёт по «хотя бы 1 × 20».

---

## Итерация 8 — 2026-07-17 (импорт истории из отчёта, meta в бэкапе, формат отчёта, фикс пароля)

### Сделано
- [x] Пароль: запасной чистый JS SHA-256 + trim ввода — вход «1234» работает в любом контексте (v0.47.1).
- [x] Бэкап теперь включает meta (customTasks/цели, userSups, supPeriods) — раньше сохранялись ТОЛЬКО дни, цели/БАДы терялись. `backupMeta()`.
- [x] Импорт применяет meta (`mergeMetaIn`): цели и кастомные БАДы по id без дублей, курсы по ключу. Работает и в merge, и в replace.
- [x] applyImport('replace') теперь прогоняет дни через repairDay (восстановление массивов) — защита от падения на неполной структуре.
- [x] Формат отчёта: строка «Сформирован: DD.MM.YYYY HH:MM · vX» в шапке + «· изменён DD.MM.YYYY HH:MM» по каждому дню (из lastUpdated). Хелпер `fmtTs`.
- [x] Конвертация текстового отчёта пользователя (15 дней + 5 целей) в JSON: перемап старых названий→ID, оценки /10→/5, даты, lastUpdated. Проверено импортом в браузере (дни/цели встают, переживают перезагрузку, round-trip бэкапа, LWW безопасен, 0 ошибок).
- [x] Версия 0.48.0.

### Важно (объяснить пользователю)
- Баланс после импорта (~95) НЕ совпадёт со старым (164.75): текстовый отчёт лоссовый (15 из 21 дня, без опций/БАДов), и правила начисления с тех пор изменились. Сырые данные дня (галочки, намазы, вода, сон, энергия, заметки, оценки) импортированы верно — баланс это производная величина, пересчитывается по текущим правилам.

---

## Итерация 9 — 2026-07-18 (финальный штрих: шапка, ментальное состояние, безопасность)

### Сделано
- [x] Шапка перестроена: крупнее и равномернее. Намазы — 5 пилюль с ВРЕМЕНАМИ по центру (готовые ✓, следующий подсвечен оранжевым), кольцо/дата/баланс крупнее, «Жизнь» компактной строкой внизу рядом с сериями. Шестерёнка увеличена (34→42px).
- [x] Ментальное состояние: убран дубль «Летал в мыслях» (drift) — оставлен «Фокус». Добавлена «🌀 Прокрастинация» с записями (сила/время/комментарий, несколько за день — механика как у замеров). Старый fact-пункт procrast из «Самоконтроль» убран (не дублировать). В отчёте новая строка «Прокрастинация».
- [x] «Наспех» переименован в нейтральное «≈ Неточно» (данные под сомнением, не приоритетны в анализе). Плашка в отчёте переформулирована.
- [x] Ретро-правки: при редактировании НЕ сегодняшнего дня фейковое время отметок (rec) не записывается — плюс в отчёте правдивая строка «изменён ДАТА ВРЕМЯ» из lastUpdated. Отчёт больше не соврёт о времени.
- [x] Безопасность данных подтверждена тестом: пустое устройство при синхронизации НЕ стирает полное (union + pull-first). Проверены сценарии empty↔full и конкурентная правка.
- [x] Охота на баги: все 74 обработчика из HTML определены — битых кнопок нет.
- [x] Версия 0.49.0.

### Не делал (осознанно / жду ввода)
- Полная смена палитры (синий/чёрный/салатный) — оставил тёмно-зелёную (пользователь: «привлекательный… ты сам реши»); готов сделать вариант.
- Замена confetti на «волны» — отложено (не критично).
- «Не работает на телефоне» — вероятно старая версия без фикса пароля (0.47.1) не задеплоена; нужен ввод, что именно видно на телефоне.

---

## Итерация 10 — 2026-07-18 (почти чёрная тема, диагностика «телефона»)

### Сделано
- [x] Тема переведена в почти чёрную нейтральную гамму: фоновые токены #0E120F/#12180F/#161D17/#19211A/#232B24/#283029 → нейтрально-чёрные (#08090B/#0E0F13/#131419/#16171D/#20232A/#262A32). Зелёные/золото/красные акценты не тронуты — теперь ярче выделяются на чёрном (дофаминовый эффект). Версия 0.50.0.

### Диагностика «не работает на телефоне»
- По скриншотам: пользователь открывает присланный index.html ВНУТРИ просмотрщика файлов мессенджера (урезанный webview: заголовок «index.html» + кнопка скачивания). Там замок показывается, но после логина — пустой тёмный экран (webview не даёт приложению работать).
- Проверено: в мобильной эмуляции (iPhone UA, touch) приложение рендерится и логинится без ошибок; живой Netlify (curl) = v0.47.1 с фиксом пароля и dashHTML. Код НЕ сломан.
- Решение — инструкция пользователю: приложением пользоваться ТОЛЬКО через Netlify-ссылку в настоящем браузере (Chrome), а присланный файл — только заливать в Netlify, не открывать напрямую.

---

## Итерация 11 — 2026-07-18 (настраиваемые времена чекинов)

### Сделано
- [x] Времена двух чекинов теперь настраиваются: meta.checkinDay (12:00) / checkinEve (22:30). В настройках Таб 2 — блок «⏰ Времена чекинов» с двумя time-инпутами. Подписи пунктов «Дневной/Вечерний чекин» в «Самоконтроле» и в отчёте показывают выбранные времена (dispLabel). Времена входят в backupMeta и импорт.
- [x] Поток мыслей — отдельное поле НЕ добавлял (пользователь решил не надо).
- [x] Версия 0.51.0.

---

## Итерация 12 — 2026-07-19 (финальное большое обновление, v0.52.0)

### База
- Ветка `claude/unfilled-days-cross-formula-tnetwb` была отрезана от main (v0.44.0);
  приведена к реальному задеплоенному состоянию v0.51.0, поверх него — правки ниже.

### Сделано
- [x] **Формула крестика (главная задача).** Раньше `isSkippedDay` считал `loggedSections`
  (широкие «секции» — все галочки = 1). День с 10 галочками, но без остального = крестик →
  «крестики везде». Введён `answersCount(d)` — поштучный счёт ответов (галочки, намазы,
  ползунки, поля, записи логов, сон, замеры, самооценка). Порог < 5. `loggedSections`
  оставлен только для денежной логики (dayMoney, closed-порог) — исторический баланс не сдвинут.
  Проверено тестом: день с 10 галочками (loggedSections=1, answersCount=10) больше НЕ крестик.
- [x] **Крестик красный** (#E0795B) + красно-пунктирная рамка ячейки. Пропущенные дни
  **исключены из статистики**: `calcStreak` (серия 5/5) теперь нейтрально пропускает пустые
  дни — забытый день не обнуляет реальную серию. `cleanStreak` уже не рвётся на пустых.
- [x] **Раскладка без «дыр».** `.cats-wrap` переведён с CSS-grid (построчно → дыры под
  короткими блоками) на multicolumn (`column-count:2` + `break-inside:avoid`) — блоки
  складываются вплотную «как пазл».
- [x] **Убран «☀️ Зубы (утро)»** (`teeth_am`) из CATEGORIES и утренней группы ритуалов.
  Вечерние зубы и лимфодренаж на месте.
- [x] **Микро-эффекты выполнения.** Волна от точки касания (камень в воду) + акценты
  вразнобой (мягкое свечение / зелёный лепесток / кольцо-пульс), микровибрация. Пул
  чередуется псевдослучайно. `_lastTap` ловит точку касания через pointerdown. Уважает
  prefers-reduced-motion. `confetti()` заменён на спокойный «bloom» (свечение+волны+лепестки),
  без падающего мусора — по просьбе пользователя (не любит конфетти).
- [x] **Счётчик «0/2» понятнее.** Пункты «по факту» вынесены отдельной группой под подписью
  «по факту · не входит в счёт»; на счётчик добавлен title с пояснением. Логика баллов не тронута.
- [x] Аудит данных: LWW-слияние/импорт/Google Drive-синхронизация проверены — пустое
  устройство НЕ затирает облако (it>lt), union ключей сохраняет облачные дни, repairDay
  не теряет неизвестные поля. Мёртвых кнопок нет (77 обработчиков — все определены).
- [x] Размер файла 170 KB (лёгкий, без раздувания). Версия 0.52.0.

### Проверка
- node --check: OK. Playwright (Chromium, desktop 1280 + mobile 400): рендер без pageerror,
  крестик-формула/цвет/раскладка/группировка/эффекты — все ассерты зелёные.
  (Консольные ERR_CONNECTION_RESET — заблокированные Google-скрипты в песочнице, не баг.)

---

## Итерация 13 — 2026-07-19 (финал: чёрная тема, эффекты, конструктор, v0.53.0)

### Сделано
- [x] **Тема — истинно чёрная.** Фоновые токены нейтрализованы (был чёрно-синий отлив):
  #08090B→#000000, #0E0F13→#0B0B0C, #131419→#151517, #16171D→#1A1A1D,
  #20232A→#2C2C31, #262A32→#34343B; зелёные rgba-подсветки → мятные (70,217,140).
- [x] **Больше контраста + блоки очерчены.** Каждая категория — карта: `.cat-block`
  получил рамку/фон, `.cat-header` — бар. Выполненные пункты ярче: `.row.on`/`.task-check.on`/
  `.check-box.on` — сильнее зелёная заливка и рамка. Зелёный акцент чуть светлее (#46D98C).
- [x] **Эффекты переписаны.** Волна теперь из ЦЕНТРА пункта в 70% случаев (30% — от касания;
  ловим rect задетого элемента в pointerdown). Мягкая палитра из 7 оттенков (мята, голубой,
  лаванда, песок, тиффани, белёсый, розовый) — не только зелёный. Акценты вразнобой: расцвет
  лепестков/искр ПО ВСЕМУ пункту, мягкое свечение, кольцо-пульс. confetti → щедрый спокойный bloom.
- [x] **Конструктор привычек.** meta.customItems «вживляются» в CATEGORIES (applyCustomItems),
  поэтому баллы/счётчики/отчёт/крестик подхватывают их сами. UI в Настройках → Таб «Кастом»:
  блок (любая категория + Ритуал Утро/Вечер), название, тип (об/жел/факт), баллы, штраф, дни недели.
  Добавление/удаление на лету. Входит в backupMeta/mergeMetaIn. Проверено: кастомный пункт
  начисляет свои баллы (3.5→5.5) и корректно удаляется.
- [x] Ещё раз проверена синхронизация/импорт (LWW): пустое устройство не затирает облако.
  Размер 180 KB (цель 180-190). Версия 0.53.0.

### На заметку (сказать пользователю)
- Определения кастомных привычек живут в meta (как настройки/кастом-задачи) и переносятся
  через копию/импорт, а НЕ через Google Drive (Drive синхронизирует только дни). Отметки по
  дням синхронизируются. Если нужно авто-перенос определений между телефоном и ноутом через
  Drive — это отдельная доработка (расширение формата облачного файла), делать по запросу.

---

## Итерация 14 — 2026-07-19 (намаз-цветы, клип-эффекты, свечение серии, v0.54.0)

### Сделано (последнее перед заморозкой до 15 августа)
- [x] **Эффекты клипуются границами пункта.** Была проблема: волна расширялась в 6.5× и
  вылезала за пределы пункта, эффект ощущался «оторванным» и неконтрастным. Теперь fxBurst
  создаёт контейнер `.fx-box` строго по rect пункта с `overflow:hidden` — волна + контрастная
  вспышка-заливка (mix-blend screen) + лепестки/искры рисуются ВНУТРИ и клипаются границами.
  Задевает только сам пункт, чётко и контрастно. Проверено: box совпадает с пунктом, overflow hidden.
- [x] **Постоянные цветы на намазе.** При выполнении намаза внутри кнопки появляются ~15
  мелких разноцветных цветов/лепестков (стабильные позиции по seed из id) и ОСТАЮТСЯ, пока
  намаз отмечен (prayerBloomHTML в namazHTML, подпись обёрнута в .pb-fg над цветами,
  .prayer-btn получил position:relative;overflow:hidden). Проверено: 15 цветов, персистят при перерисовке.
- [x] **Свечение серии подключено.** Классы .streak-tag.tier1/2/3 существовали в CSS, но не
  применялись. Теперь streakTier(n): 3→пульс, 7→вспышка, 30→золотое свечение — на «серия» и
  «без срывов». Проверено: tier3 применяется на серии 49.
- [x] Синтаксис OK, 80 обработчиков (все определены), 180 KB, 0 ошибок рендера. Версия 0.54.0.

## Итерация 15 — 2026-07-19 (последний штрих перед заморозкой, v0.55.0)
- [x] Календарь вперёд: periodEnd = сегодня + 45 дней (было +14). По просьбе — «месяц и 15 дней».
  Скользящее окно, жёсткой даты конца по-прежнему нет. Проверено: до 2 сен, 0 ошибок. Версия 0.55.0.
- После этого — ЗАМОРОЗКА до 15 августа (исключение только критическая инфраструктура).

## Итерация 16 — 2026-07-19 (КРИТ. ИСКЛЮЧЕНИЕ: защита импорта, v0.56.0)
- Диагноз: пользователь импортировал внешне-сконвертированный JSON, где все дни были слиты
  в один (2026-07-15). Импорт залил как есть → один день полный, остальные пустые → красные
  кресты везде (кресты КОРРЕКТНЫ: дни реально пустые). Формула крестика не при чём.
- [x] Защита импорта (критическая инфраструктура): sanitizeImportDays() пропускает только
  ключи-даты ГГГГ-ММ-ДД (мусорные ключи не превращаются в «дни»); если валидных дней 0 —
  импорт останавливается, данные не трогаются. Превью теперь показывает ДИАПАЗОН ДАТ и
  предупреждает, если день всего один («похоже, слито в один день, не загружай»). onFile,
  importPaste, pendingImportHTML. Проверено: обычный бэкап грузится, слитый файл предупреждает,
  мусор отклоняется. Версия 0.56.0.
- Восстановление данных пользователя — по его файлу (следующий заход).
- НЕ трогал (заморозка, косметика → 15 авг): перенос кнопки «Отчёт» в настройки; дата в хронологии.

## Итерация 17 — 2026-07-19 (контекстный вид «только сейчас», v0.57.0)
- [x] БАГ: дубль warmwater («Тёплая вода») был в двух блоках → считался дважды в баллах и %.
  Убран из «Здоровье», остался в «Ритуалы: утро». Теперь считается один раз.
- [x] Контекстный вид по ДНЮ НЕДЕЛИ: пункты по графику (workout Пн/Ср/Пт, nails, hair, laundry,
  phoneclean) показываются только в свой день. Внеграфиковые невыполненные — прячутся из блоков
  и собраны в один общий блок «➕ Сделал вне графика» (тап → отметить, если сделал вне плана).
  Выполненное вне графика показывается в своём блоке с тегом «вне графика».
- [x] Пустые сегодня блоки не рисуются (напр. «Уход по сунне» в дни без ногтей/волос) — меньше шума.
- [x] «Детали дня» открыты по умолчанию (не искать внизу, не открывать каждый раз).
- [x] Убран чип «+N доп» из шапок блоков (внеграфиковое теперь в общем «➕»).
- Проверено: дедуп (warmwater 1×), off-список по дню верный, пустой блок скрыт, отметка вне
  графика возвращает блок и убирает из «➕», детали открыты, 0 ошибок. 184 KB. Версия 0.57.0.
- НЕ делал (осознанно): жёсткое «не засчиталось после 13:00» по времени суток — ломает правку
  прошлых дней и наказывает за дело, сделанное чуть позже; предложил мягкий вариант. Структурное
  слияние мелких блоков — авто-скрытие пустых уже убирает шум, полное слияние оставил, чтоб не рисковать.
